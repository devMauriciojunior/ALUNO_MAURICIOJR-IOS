// for (inicialização; condição; incremento) {
//     condição a ser executada
// }

const fruta = ['bananna', 'maça', 'melancia', 'uva']
for (let x = 0; x < fruta.length; x++) {
    console.log(`Nome: ${fruta[x]}`)
}
