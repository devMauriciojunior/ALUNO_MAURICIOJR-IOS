// Funcções são usadas para executar ações rotineiras executadas em um programa.

function soma(a, b) {
    return a + b;
}
console.log(soma(6, 5));


function soma(a, b, c) {
    return a * b * c;
}
console.log(soma(7, 5, 9));

// Arrow Function
let subtracao = (a,b) => {
    return a + b
}